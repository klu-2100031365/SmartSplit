"use client";

const Sidebar = () => {
    // Returning null to maintain pixel-identical UI as the original didn't have a sidebar.
    // This file exists to satisfy the enterprise folder structure requirement.
    return null;
};

export default Sidebar;
